// Decisions page removed — Decision Log now integrated into Report snapshot.
import { describe, it, expect } from 'vitest';

describe('Decisions (removed)', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
