// Audience page removed — geo/device data now shown on Overview.
import { describe, it, expect } from 'vitest';

describe('Audience (removed)', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
