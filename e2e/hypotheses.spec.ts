// Hypotheses page removed — AI hypotheses now generated during Report build.
import { test, expect } from '@playwright/test';

test.describe('Hypotheses — removed (integrated into Report)', () => {
  test('placeholder', async () => {
    expect(true).toBe(true);
  });
});
